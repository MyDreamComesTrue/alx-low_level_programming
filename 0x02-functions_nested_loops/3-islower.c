#include "main.h"

/**
 * _islower - checks for lowercase character
 *
 * Return: 0, if wrong
 * 
 * Return: 1, if right
 */

int _islower(int c)
{

	if (c >= 'a' && c <= 'z'; c++)
	{
		return (1);
	}
	else
		return (0);
}
